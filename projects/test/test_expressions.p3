
pub fn testExpressions() {
    const a = 2
    const a2 = 10 != a and true

    assert 10 + 10 == 20
    assert 10 - 9  == 1
    assert 2 * 3   == 6
    assert 6 / 3   == 2
    assert 10 % 3  == 1

    assert 10 > 9
    assert 10 >= 10
    assert 10 < 11
    assert 11 <= 11

    //assert @ult(10, 11)

    assert 10 == 10
    assert 10 != 9

}
